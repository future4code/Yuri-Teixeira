export type userType = {
  id: string;
  email: string;
  name: string;
  password: string;
};

export type recipeType = {
  id: string,
  title: string,
  description: string,
  creationDate: string
}
